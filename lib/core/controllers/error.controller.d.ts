import { Request, Response, NextFunction } from "express";
export declare const errorController: (error: any, req: Request<import("express-serve-static-core").ParamsDictionary>, res: Response<any>, next: NextFunction) => void;
